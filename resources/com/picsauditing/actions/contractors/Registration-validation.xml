<!DOCTYPE validators PUBLIC "-//Apache Struts//XWork Validator 1.0.3//EN"
        "http://struts.apache.org/dtds/xwork-validator-1.0.3.dtd">

<validators>

    <!-- This references validation in ContractorAccount-validation.xml -->
    <field name="contractor">
        <field-validator type="visitor">
            <message></message>
        </field-validator>
    </field>

    <!-- This references validation in User-validation.xml -->
    <field name="user">
        <field-validator type="visitor">
            <message></message>
        </field-validator>
    </field>

    <field name="confirmPassword">
        <field-validator type="requiredstring" short-circuit="true">
            <message>${getText("JS.Validation.Required")}</message>
        </field-validator>
        <field-validator type="fieldexpression">
            <param name="expression"><![CDATA[user.password == confirmPassword]]></param>
            <message>${getText("JS.Validation.PasswordsMustMatch")}</message>
        </field-validator>
    </field>

</validators>