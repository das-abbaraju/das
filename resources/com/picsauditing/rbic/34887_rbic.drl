package com.picsauditing.rbic
dialect "java"

import com.picsauditing.rbic.ContractorModel;
import com.picsauditing.jpa.entities.LowMedHigh;

rule "workingOnHighFloor"
    when
        $con : ContractorModel( findQuestionAnswerAsNumber(17080) > 9)
    then
        $con.updateCriteriaLimitForContractor(522, 34887, 3000123);
    end
rule "workingOnLowFloor"
    when
        $con : ContractorModel(findQuestionAnswerAsNumber(17080) <= 9)
    then
        $con.updateCriteriaLimitForContractor(522, 34887, 1000123);
    end

