<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/rabbit"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xmlns:context="http://www.springframework.org/schema/context"
             xmlns:tx="http://www.springframework.org/schema/tx"
             xmlns:bean="http://www.springframework.org/schema/beans"
             xsi:schemaLocation="
       http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
       http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.0.xsd
       http://www.springframework.org/schema/rabbit http://www.springframework.org/schema/rabbit/spring-rabbit-1.1.xsd
       http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd">

    <context:property-placeholder location="classpath:amqp_connection.properties"/>
    <connection-factory id="connectionFactory" host="${rabbit.host}" port="${rabbit.port}" username="${rabbit.username}" password="${rabbit.password}"/>

    <bean:bean id="uniqueQueueName" class="com.picsauditing.messaging.RandomQueueName" />

    <admin id="amqpAdmin" connection-factory="connectionFactory"/>

    <queue id="translation-usage" name="#{uniqueQueueName.queueName}"/>
    <direct-exchange name="test-translation-usage-exchange">
        <bindings>
            <binding queue="translation-usage"/>
        </bindings>
    </direct-exchange>

    <template message-converter="messageConverter" id="TranslationUsageTemplate" connection-factory="connectionFactory" exchange="test-translation-usage-exchange" queue="#{uniqueQueueName.queueName}"/>
    <beans:bean id="TranslationUsagePublisher" class="com.picsauditing.messaging.PublisherRabbitMq" scope="prototype">
        <beans:property name="amqpTemplate" ref="TranslationUsageTemplate" />
    </beans:bean>

    <beans:bean id="defaultJavaTypeMapper" class="org.springframework.amqp.support.converter.DefaultJavaTypeMapper">
    </beans:bean>

    <beans:bean id="messageConverter" class="org.springframework.amqp.support.converter.JsonMessageConverter">
        <beans:property name="javaTypeMapper" ref="defaultJavaTypeMapper"/>
    </beans:bean>

</beans:beans>
