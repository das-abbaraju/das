<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="http://www.springframework.org/schema/beans 
	   		http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
	   		http://www.springframework.org/schema/context
	   		http://www.springframework.org/schema/context/spring-context-3.0.xsd
			http://www.springframework.org/schema/mvc 
			http://www.springframework.org/schema/mvc/spring-mvc-3.0.xsd">

	<!-- Scans the classpath for annotated components that will be auto-registered 
		as Spring beans. For example @Controller and @Service. Make sure to set the 
		correct base-package -->

	<context:component-scan base-package="com.klark" />
	<context:component-scan base-package="com.klark.user" />
	<context:component-scan base-package="com.klark.user.model" />
	<context:component-scan base-package="com.klark.controller" />
	<context:component-scan base-package="com.klark.user.service" />
	<context:component-scan base-package="com.klark.util" />

<!-- Activates various annotations to be detected in bean classes -->
    <context:annotation-config />

	<!-- Configures the annotation-driven Spring MVC Controller programming 
		model. Note that, with Spring 3.0, this tag works in Servlet MVC only! -->
	<mvc:annotation-driven />

	<!-- Load Hibernate related configuration -->
	<import resource="classpath:/hibernate-context.xml" />

    <mvc:annotation-driven />

<bean id="mail" class="com.klark.common.Mail">
    <property name="host" value="${mail.host}"/>
    <property name="port" value="${mail.port}"/>
    <property name="username" value="${mail.username}"/>
    <property name="password" value="${mail.password}"/>
    <property name="from" value="${mail.from}"/>
    <property name="regSubject" value="${mail.reg.subject}"/>
    
    
</bean>


<context:property-placeholder location="classpath:messages.properties" />

</beans>
