<?xml version="1.0"?>
<project name="diagnostic" default="all" basedir=".">
	<property environment="env"/>
		

  <target name="systemProperties">
    <echo message="$${java.version}:
        ${java.version}"/>
    <echo message="$${java.vendor}:
        ${java.vendor}"/>
    <echo message="$${java.vendor.url}:
        ${java.vendor.url}"/>
    <echo message="$${java.home}:
        ${java.home}"/>
    <echo message="$${java.vm.specification.version}: 
        ${java.vm.specification.version}"/>
    <echo message="$${java.vm.specification.vendor}: 
        ${java.vm.specification.vendor}"/>
    <echo message="$${java.vm.specification.name}: 
        ${java.vm.specification.name}"/>
    <echo message="$${java.vm.version}: 
        ${java.vm.version}"/>
    <echo message="$${java.vm.vendor}: 
        ${java.vm.vendor}"/>
    <echo message="$${java.vm.name}: 
        ${java.vm.name}"/>
    <echo message="$${java.specification.version}: 
        ${java.specification.version}"/>
    <echo message="$${java.specification.vendor}: 
        ${java.specification.vendor}"/>
    <echo message="$${java.specification.name}: 
        ${java.specification.name}"/>
    <echo message="$${java.class.version}: 
        ${java.class.version}"/>
    <echo message="$${java.class.path}: 
        ${java.class.path}"/>
    <echo message="$${java.library.path}: 
        ${java.library.path}"/>
    <echo message="$${java.ext.dirs}: 
        ${java.ext.dirs}"/>
    <echo message="$${java.io.tmpdir}: 
        ${java.io.tmpdir}"/>
    <echo message="$${os.name}: 
        ${os.name}"/>
    <echo message="$${os.arch}: 
        architecture: ${os.arch}"/>
    <echo message="$${os.version}: 
        ${os.version}"/>
    <echo message="$${env.CATALINA_HOME}: 
    	${env.CATALINA_HOME}"/>
  </target>

  <target name="all" depends="systemProperties"/>

</project>
